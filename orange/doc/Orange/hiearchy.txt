

Orange
   core
      orange; counters; serverfiles
   data
      Table; Instance; Value; Domain
      feature (Discrete, Continuous, String; Python)
      io; sampling
   statistics
      estimate; distributions; evd
   feature
      scoring; selection; continuization; discretization; 
      imputation; construction; interactions
   distance
      instances; features
   preprocessing
      outliers; filter; scaling
   classification
      trees; rules; lookup; bayes; svm; logreg; knn; majority; wrappers;
      multitarget; multilabel; multiclass
   regression
      majority; pade; linreg; trees
   ensamble
      forest; boosting; bagging
   clustering
      kmeans; hierarchical; consensus
   projection
      correspondence; pca; freeviz; mds; som
   associate
      rules; itemsets
   network
      graph
   evaluation
      scoring; testing
   optimization
      tuning
   multilabel
      br; powerset
